# Приёмочный чек-лист

Проверка воспроизводимости от 12 сентября 2026, исходный коммит `06c05cc`. В запросе упомянуты 11 пунктов, но сохранённое исходное задание в [requirements.md](requirements.md) содержит **14**. Ниже сохранена его нумерация: проверены первые 11 и дополнительные 12–14, без сокращения требований.

Серверные проверки выполнены в чистой Git-копии: **8/8 CTest**, дополнительно **6/6 проверок JS с заглушками**. HTTP-проверка не означает, что проверены нажатия или вёрстка в браузере. Точные команды, версии и результаты — [verification.md](verification.md).

| № | Требование | Место реализации | Подтверждение | Пробелы и границы |
| --- | --- | --- | --- | --- |
| 1 | Около 1000 квартир/домов со всеми характеристиками и несколькими фото | `src/seed.cpp`, `sql/001_initial.sql`, `public/images/` | `database`, `http_smoke`; новая база чистой копии: 1000 объектов, 1000 объявлений, 3000 записей фото | Четыре повторяемые иллюстрации, а не реальные фото адресов; даты создания зависят от первого запуска |
| 2 | Каталог, отдельная страница, фото и местоположение | `src/main.cpp`, `src/database.cpp`, `public/index.html`, `public/listing.html`, `public/app.js`, `public/listing.js` | `http_smoke`: список, подробности, прямой HTML-адрес, 404, JPEG; отдельный HTTP-прогон чистой копии | Адрес/район вместо точной карты допустимы заданием. Финальное отображение и прямое обновление браузером не проверены |
| 3 | Аренда отдельно от продажи | `src/seed.cpp`, `src/main.cpp`, `src/database.cpp`, `public/app.js`, `public/common.js` | `database`, `http_smoke`: разделение/фильтр сделки; цена аренды в месяц | Переключение вкладок в актуальном браузере не проверено |
| 4 | Совместные фильтры, четыре сортировки, пагинация и обоснование | `src/database.cpp`, `src/main.cpp`, `sql/002_catalog_indexes.sql`, `public/app.js`; `docs/database-guide.md`, `docs/query-plans.md` | `http_smoke`: весь порядок 1000 id, равные значения, совместные условия, границы, страницы без дублей; `database` | SQL ORDER BY до LIMIT/OFFSET, дополнительный id стабилизирует равенства. Планы зависят от данных/SQLite; скорость больших наборов не измерена. URL/клики в браузере не проверены |
| 5 | Регистрация, вход/выход, безопасные пароли и сессии | `src/security.cpp`, `src/auth.cpp`, `src/accounts.cpp`, `sql/003_users_bookings.sql`, `public/auth-client.js`, `public/auth-page.js` | `security`, `auth_http`: Argon2id, дубли email, пароль, сессии, CSRF, выход/лимиты; регистрация и повторный вход в чистой копии | Сессии одного процесса, 12 часов, пропадают при перезапуске. Нет подтверждения email/восстановления пароля; HTTPS не развёрнут. Реальные формы последней версии не проверены |
| 6 | Только вошедший пользователь бронирует доступное | `src/auth.cpp`, `src/accounts.cpp`, `public/listing.js` | `auth_http`: 401 гостю, 404, успех и повтор; проверка продавца в `lifecycle_http` | Учебное бронирование без платежей и юридической сделки; финальная кнопка в браузере не проверена |
| 7 | Ровно один победитель одновременного бронирования, гарантия БД | `src/accounts.cpp` (BEGIN IMMEDIATE, условный UPDATE, INSERT, откат/повторы), `sql/003_users_bookings.sql` (уникальные активные записи) | `auth_http`: синхронные HTTP-запросы разных пользователей, 201/409, одна запись/владелец/status, искусственный откат; `booking_database` | Два рабочих потока/отдельные соединения; нагрузочное и многопроцессное испытание не выполнялось, сама защита задаётся SQLite |
| 8 | «Мои бронирования» | `src/auth.cpp`, `src/accounts.cpp`, `public/bookings.html`, `public/bookings.js` | `auth_http`, `lifecycle_http`: только свои записи, цена/даты/статус; запись после перезапуска чистой копии | Кабинет пока без пагинации; отображение актуального интерфейса не проверено |
| 9 | Освобождение аренды делает то же объявление доступным | `src/accounts.cpp`, `src/auth.cpp`, `public/bookings.js` | `lifecycle_http`: аренда → освобождение → другой арендатор, история, повтор/гонки и запоздалое освобождение, откат | Подтверждение кнопкой в браузере остаётся непроверенным |
| 10 | Перепродажа новым объявлением, старая история и разделение недвижимости/объявлений | `sql/001_initial.sql`, `sql/004_resale_origin.sql`, `src/accounts.cpp`, `src/auth.cpp`, `public/bookings.js` | `lifecycle_http`: новая цена/id, тот же property_id, старые цены, продавец/происхождение, недопустимые цены, ровно одна конкурентная перепродажа, полный откат, повторный генератор | Характеристики/фото не копируются. Форма цены и ссылка нового объявления в актуальном браузере не проверены |
| 11 | Только соответствующий владелец освобождает/перепродаёт | `src/auth.cpp` (пользователь из сессии, CSRF), `src/accounts.cpp` (владелец/тип/статус конкретного бронирования) | `lifecycle_http`: чужой пользователь, гость, неверный тип, CSRF, повторные/запоздалые запросы | Два независимых браузерных окна для последнего UI не проверены; автоматические HTTP-клиенты действительно независимы |
| 12 | Избранное, история просмотров, рекомендации по районам/ценам | `sql/005_personalization.sql`, `src/personalization.cpp`, `src/auth.cpp`, `public/personal*.js`, `public/recommendations.js` | `personalization_database`, `personalization_http`: уникальность, гонки, права, окно 30 минут, известный малый набор, веса 3/1, отдельные цены сделок, исключения и гостевая подборка; JS-тесты состояния избранного | Без ML; максимум 200 сигналов/24 результата, проход по доступным кандидатам. История агрегирована. Браузерная проверка актуального UI не выполнена |
| 13 | Сохранение после перезапуска, генератор без дублей/сброса | `src/database.cpp`, `src/seed.cpp`, миграции 001–005 | `database` и четыре HTTP-теста; отдельный прогон: все строки семи таблиц совпали после генератора и перезапуска, повторный вход успешен | Сессии намеренно не сохраняются. Рабочая база не использовалась для тестовых изменений |
| 14 | README с установкой, сборкой, созданием БД, запуском и тестами | `README.md`, `CMakeLists.txt`, `src/main.cpp` | Чистый `git archive HEAD`: CMake, сборка, `--init-db`, CTest, сервер/HTTP; SQL, генератор и ресурсы присутствуют в Git | Проверен Mac с уже установленными зависимостями; установка на чистой ОС и Linux/Windows не проверены. Версии Homebrew не закреплены. Дополнительный JS-тест выполнен Node REPL, CLI Node не установлен |

## Перед сдачей

- Пройти ручной сценарий последнего интерфейса на широком/узком экране и с независимыми аккаунтами: [verification.md](verification.md). Не отмечать его выполненным по серверным тестам.
- Для локальной сборки на проверенном Mac блокирующих ошибок не обнаружено. На другом компьютере установить зависимости по README и повторить команды: текущая проверка этого не заменяет.
- GitHub-репозиторий ещё не создан и push не выполнен в рамках этой задачи; это отдельное действие по разрешению пользователя, необходимое для срока сдачи.
- Реальные платежи, промышленное развёртывание и ML не входят в текущую учебную реализацию. Это не следует выдавать за реализованные возможности на защите.

## Дополнение: демоданные версии 2 (13 сентября 2026)

Миграция 6 и отдельный генератор/безопасное обновление описаны в [demo-generator.md](demo-generator.md). Новый полный прогон — 10/10 CTest и 7/7 JS-проверок, прежние гарантии проверены снова. Статистика: 994 объекта обновлены, 6 с историей сохранены; повторное обновление — 0 изменений. Условия воспроизводимости и непроверенный браузер остаются прежними. Основной процесс пока не перезапущен ради сохранения сессий: новое поле ремонта проверено на отдельном сервере; актуальный статус — в verification.md.
