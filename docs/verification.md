# Фактически выполненные проверки

## Этап 5 — 12 сентября 2026

Исходная точка — `78445f7`, завершённый этап 4. Прочитаны AGENTS.md, требования, план, код, схема и прежний отчёт. Проверены Git status/diff, история коммитов, процессы и user_version. Работа продолжена без сброса файлов, повторного создания проекта или очистки данных. После сообщения о продолжении отдельно подтверждено: миграция 5 уже применена, сервер PID 50340 работает на 8080; повторно он не запускался.

### Сначала проверен четвёртый этап

В отчёте этапа 4 найдены конкретные результаты: **6/6 тестов за 8,35 с**, пять пар конкурентных освобождений и пять перепродаж, откат при ошибках. Перед добавлением пятого этапа заново выполнен `ctest --test-dir build --output-on-failure`: **6/6 прошли за 8,37 с**. Подтверждены аренда → освобождение → новый арендатор, запоздалый повтор, перепродажа, исходные цены/история, ограничения владельца/типа/CSRF, конкуренция и полный откат. Серверных ошибок прежнего поведения не обнаружено.

Попытка браузерной проверки новых кнопок этапа 4 через Safari не удалась: инструмент сообщил, что **сессия приложения остановлена пользователем**. Действия двумя пользователями в браузере не выполнены; работа продолжена через отдельные тестовые базы и HTTP.

### Итоговая сборка и весь актуальный набор тестов

`cmake --build build -j 4` завершился успешно. Добавлены `personalization_database` и `personalization_http`; прежние тесты сохранены, ожидаемая текущая версия схемы изменена на 5. Старые миграции 1–4 и генератор не изменены.

После финальной правки возврата с формы входа выполнен `ctest --test-dir build --output-on-failure`: **8/8 прошли, итог 11,86 с**, ни одного пропуска или сбоя. Ниже результаты именно последнего прогона; время тестов не является замером производительности приложения.

| CTest | Результат | Время | Что проверяет |
| --- | --- | --- | --- |
| `database` | Passed | 0,08 с | Каталог, схема/ограничения/FK, 1000 объектов, сохранение правок и повторный генератор |
| `security` | Passed | 0,25 с | Argon2id, сессии, выход/истечение и лимиты |
| `booking_database` | Passed | 0,04 с | Миграция старой базы, владелец, уникальность активного бронирования и история |
| `personalization_database` | Passed | 0,01 с | Рекомендации на небольшом известном наборе, веса/цены/районы/разделение сделок и ограничения объёма |
| `http_smoke` | Passed | 1,12 с | Совместные фильтры, четыре сортировки, страницы, фото, 400/404 и сохранение |
| `auth_http` | Passed | 3,23 с | Настоящие HTTP входа/выхода/CSRF, гонка бронирования, откат, приватность и перезапуск |
| `lifecycle_http` | Passed | 3,73 с | Освобождение, повторная аренда, перепродажа, гонки, запоздалые повторы и откат |
| `personalization_http` | Passed | 3,40 с | Избранное/просмотры/пагинация/CSRF/приватность, конкурентные запросы, миграция и перезапуск |

Все изменяющие проверки используют `:memory:` либо отдельный временный файл SQLite. HTTP-тесты запускают настоящий сервер на свободном порту с двумя потоками; рабочая `data/apartments.sqlite3` не используется для тестовых действий.

### Конкретные новые автоматические проверки

- **Миграция 4 → 5:** во временной схеме 4 заранее созданы пользователь, изменённые объект/фото, закрытое объявление, завершённое бронирование и новое объявление перепродажи с seller_user_id/source_booking_id. После миграции и генератора все прежние поля этих строк совпали, объект с существующим демоключом не продублирован.
- **Избранное:** первый PUT, повтор без смены даты, DELETE и повторный DELETE; в SQLite уникальна пара user_id/listing_id. Прямая дублирующая вставка отклонена ограничением базы. Пять пар одновременно стартующих PUT и пять пар DELETE через `threading.Barrier`: каждый ответ 200, после добавления ровно одна пара, после удаления ноль.
- **Доступ:** гостю 401 на личные API и изменяющие действия; без CSRF/с чужим Origin — 403. Подмена user_id в query/JSON, времени/счётчика просмотра, неизвестные поля, неверные id и параметры страниц/лимита отвергнуты. B не видит избранное/историю A, его DELETE того же объявления не удаляет пару A. Персональный флаг в API деталей различается для A и B.
- **Пагинация избранного:** больше 24 сохранённых, обход страниц по 5, правильный COUNT, стабильный порядок при равных датах, отсутствие дублей. Аренда, последующее освобождение и бронирование другим пользователем оставляют исходное избранное со статусом reserved; перепродажа оставляет исходный id со статусом closed и не подменяет его новым.
- **Просмотры:** повторные GET каталога, API деталей и HTML страницы не создали ни одной записи. Два синхронных POST одной пары получили 200, но ровно один `counted: true`, одна строка и один учтённый просмотр. Повтор внутри 30 минут не изменил ни время, ни счётчик. В тестовой базе время отодвинуто на 1801 секунду: следующий POST учтён, счётчик стал 2. Редактирование истории через DELETE отсутствует.
- **Маленький набор рекомендаций:** отдельно заданы аренда в Кентроне за 100000, соседний ценовой вариант 110000, дорогой 400000, варианты Арабкира и продажи. После просмотра близкая цена в том же районе выше другой цены/района. Избранный Арабкир перевешивает один просмотр Кентрона. Продажа за те же численные 100000 не получает баллы от аренды; добавление интереса к продаже за 50 млн не меняет оценки аренды.
- **Алгоритм:** увеличение view_count одного объявления до 100 не увеличивает его вес. Повторная подборка детерминирована. Проверены гость/новый пользователь без ложной персонализации, дополнение нулевыми оценками при недостатке совпадений, лимит выдачи и отсутствие дублей. Только available, без всех избранных и собственных объявлений; доступную перепродажу видит другой пользователь. Старые сигналы после 100 более новых просмотров и 100 избранных перестают влиять.
- **Блокировки:** другое соединение удерживает BEGIN IMMEDIATE; PUT и POST просмотра получают 503, не меняя снимок ни одной из семи таблиц. Ожидание PUT ограничено (в тесте проверен интервал больше 0,5 и меньше 4 с).
- **Сохранность:** после перезапуска снимки всех **семи** таблиц совпадают. Старые сессии отвергнуты; после нового входа ответы избранного/истории совпадают с прежними. Ещё один `--init-db` не меняет данные. integrity_check = ok, foreign_key_check пуст.
- **Регрессии этапов 3–4:** снова выполнены пять гонок первоначального бронирования разных пользователей (один 201 + один 409), пять гонок освобождения (200 + 409), пять перепродажи (201 + 409), совпадение победителя/цены/владельца, запоздалое освобождение без изменения нового бронирования. Триггеры в тестовой базе вызывают ошибки между шагами транзакций; сравнение всех прежних таблиц подтверждает полный откат.

### Рабочая база, запросы и запуск

- Перед обновлением проверена схема **4**. Сервер PID 44473 остановлен с разрешением, порт 8080 освобождён. Через SQLite Backup API создана проверенная копия **`data/backups/before-stage5-20260912-184637.sqlite3`**.
- Один `./build/apartment_server --init-db` выполнил только миграцию **5**. Сравнение с копией подтвердило полное совпадение всех прежних строк/столбцов в properties, listings, property_photos, users и bookings. Новые favorites/listing_views пусты; integrity/fk в порядке.
- Сохранено **1000 объектов, 1000 объявлений, 3000 фото, 1 пользователь и 3 бронирования**. Обычный запуск с генератором также не изменил эти строки. В новой схеме семь таблиц, ни одна рабочая запись не удалена ради теста.
- Обновлённый сервер PID **50340** работает на **http://127.0.0.1:8080/**. Отдельный curl health подтвердил schema_version 5, foreign_keys true, SQLite 3.53.4 и status ok. Проверены 24 карточки каталога из 999 доступных и новый is_favorite; гостевая подборка содержит 6 разных available с оценкой 0 и personalized false, HTML подключает блок рекомендаций.
- Все семь запросов из `docs/personalization-queries.sql` выполнены через соединение **только для чтения**. EXPLAIN QUERY PLAN для списков выбрал `favorites_user_recent` и `views_user_recent` как покрывающие индексы. Личные списки до первых действий пусты — генератор не придумывает интересы пользователя.
- При финальном просмотре исправлен возврат после входа на новые страницы /favorites и /view-history: они добавлены в прежний список разрешённых внутренних адресов. Через Node VM выполнены **6 проверок** разрешённых/внешних/неизвестных адресов, все прошли. Синтаксис всех **9 JavaScript-файлов** также проверен после правки. Использовались заглушки DOM для проверки логики возврата; это не настоящий браузер и не визуальная проверка.
- `git diff --check`, проверка состава изменений и исключений Git: рабочая база, резервная копия, служебные SQLite-файлы, build и .env/ключи исключены. В коммит входят код, новая миграция, тесты и документы; прежние миграции/генератор сохранены. Публикация на GitHub не выполняется.

### Браузер и оставшиеся ограничения

**Проверка этапа 5 в браузере не выполнена.** Инструмент управления Safari недоступен из-за остановленной пользователем сессии. Не проверены реальные нажатия новых кнопок, DOM-поведение и адаптивная вёрстка. HTTP, SQL и проверка синтаксиса JS не выдаются за браузерные проверки. Ранее выполненные сценарии Safari в разделе этапа 3 относятся только к тому этапу.

Рекомендации используют фиксированные правила, не ML. Учитывается до 200 сигналов, выдаётся максимум 24 объявления; кандидаты читаются одним проходом по доступному каталогу, поэтому время растёт с его размером. Просмотренные варианты могут повторяться в подборке; избранные исключаются. История агрегирована по объявлению, без списка всех прошлых дат. Сессии в памяти одного процесса, 12 часов, после перезапуска нужен вход. Личные списки имеют пагинацию, «Мои бронирования» пока без неё.

### Короткая ручная проверка пятого этапа

1. Откройте http://127.0.0.1:8080/ как гость: в блоке «Вам может понравиться» должна быть подпись обычной подборки. Нажмите предложение входа у избранного и войдите: объявление само сохраняться не должно.
2. Сохраните объявление с карточки и другое с подробной страницы. В «Избранном» проверьте фото, цену, дату, ссылки; удалите одно. После обновления оно не должно вернуться. Для проверки второй страницы сохраните больше 24 либо откройте `/favorites?page_size=2`, сохранив три объявления.
3. Откройте подробную страницу после входа и затем «Историю просмотров». Обновите подробную страницу несколько раз: в течение 30 минут счётчик и время учтённого просмотра не меняются. Переходы по каталогу не добавляют все карточки в историю.
4. Сохраните несколько объявлений аренды одного района с близкими ценами. Вернитесь на главную: появятся подходящие варианты с объяснениями; сохранённых карточек в подборке нет. Проверьте, что цены продажи показаны за объект и оцениваются отдельно от аренды за месяц.
5. В приватном окне войдите другим пользователем: его избранное и история должны быть отдельными, у нового аккаунта подборка обычная. Изменения второго пользователя не удаляют записи первого.
6. Забронируйте сохранённое объявление, а покупку при желании перепродайте: в избранном должен остаться прежний id со статусом «Забронировано»/«Закрыто». После выхода и повторного входа списки сохраняются. Уменьшите окно и проверьте доступность кнопок/пагинации.

Действия на 8080 сохраняются в рабочей базе. Для демонстрации без её изменения используйте отдельный `--db` и свободный порт по README, предварительно проверив порт. Не запускайте второй сервер на занятом порту и не очищайте рабочую базу.

---

## Этап 4 — 12 сентября 2026

Исходная точка — `3212036` (завершённый этап 3). Продолжена существующая работа: прочитаны AGENTS.md, требования, схема, код и документы, проверены Git/diff, процессы и user_version обеих баз. Этап 4 до этого не был закоммичен. История Git и рабочие данные не сбрасывались. Применённые SQL-миграции 1–3 не редактировались.

### Сборка и автоматические проверки — выполнено

- Перед изменениями существовавшие **5/5** тестов прошли. Проект C++17 собран `cmake --build build -j 4`; новая миграция, методы C++ и маршруты скомпилированы без ошибок.
- Итоговый `ctest --test-dir build --output-on-failure`: **6/6 прошли, 8,35 с**. Это `database` (0,08 с), `security` (0,27 с), `booking_database` (0,04 с), `http_smoke` (1,02 с), `auth_http` (3,21 с), `lifecycle_http` (3,71 с). Все изменяющие сценарии используют память или отдельные временные файлы SQLite, рабочая база тестами не очищалась и не редактировалась.
- Повторно пройдены прежние проверки совместных фильтров, четырёх сортировок, стабильной пагинации, фото/деталей/404/400; Argon2id, сессий, выхода, CSRF и лимитов входа; HTTP-гонки первоначального бронирования двумя разными пользователями (пять пар 201+409), права владельца и сохранение после перезапуска.
- Новый `lifecycle_http` начинает с **реальной схемы 3** во временной базе. В ней заранее создаются объект с произвольным id, изменённые адрес/район/фото/цена, пользователь и активное бронирование. Сервер применяет миграцию 4 и генератор; все старые строки сравниваются по всем прежним полям, итог — 1000 объектов без дублирования существующего демоключа.
- Аренда → освобождение → аренда другим пользователем: то же объявление снова доступно, новая активная запись принадлежит новому пользователю, старая хранит статус released, дату завершения и исходную цену даже после изменения цены объявления в тестовой базе.
- Проверены гость (401), чужое бронирование (403), отсутствие CSRF и посторонний Origin (403), неверный тип сделки (409), отсутствующий id (404), неверный id/переполнение (400), запрещённые user_id/query/JSON-поля. GET по адресам действий отклоняется, не изменяя данные; в установленном Drogon получен 404.
- Проверены неверные цены: ноль, отрицательные, дроби, JSON `1.0`, экспоненциальная запись, bool/null/массив/объект, текст, пробелы/знак, слишком большие числа и переполнение int64. Все дают 400 без изменений. Приняты обе границы: **1** и **1 000 000 000 000**.
- Покупка → перепродажа: новый id, текущая дата, новая цена, sale/available, тот же property_id, seller_user_id владельца, source_booking_id исходного бронирования, demo_key NULL. Старое объявление изменяет только статус на closed; его цена и первоначальное бронирование сохраняют историю. Объекты и фотографии сравниваются целиком — копий/изменений нет.
- Закрытое объявление читается через API и HTML по прямому адресу и отсутствует во всех страницах доступного каталога; новое доступное объявление там присутствует. Кабинет возвращает ссылку на новое объявление. Продавцу запрещена собственная покупка (403), другой пользователь может купить и затем перепродать снова; проверена цепочка из трёх объявлений.
- Повторный запрос перепродажи не меняет строки, в том числе после покупки новым владельцем. Прямой INSERT ещё одного **закрытого** объявления с тем же source_booking_id отклонён именно уникальным индексом происхождения, независимо от ограничения незакрытых объявлений.
- Списки бронирований двух пользователей не пересекаются. После перезапуска все пять таблиц полностью совпадают с сохранённым снимком; старые сессии недействительны, после повторного входа история совпадает с прежним ответом. Дополнительный `--init-db` не изменяет строки и не дублирует перепродажи. `integrity_check = ok`, `foreign_key_check` пуст, дублей активных записей нет.
- Синтаксис всех шести JavaScript-файлов проверен через Node `vm.Script`, включая последнюю правку обновления вкладки объявления. Устаревший ответ чтения не должен перекрывать результат нового действия, а возврат к вкладке во время POST не включает повторное нажатие. Выполнение этого DOM-сценария в браузере отдельно **не проверено**.

### Конкурентные действия и полный откат — выполнено через HTTP

Реальный Drogon-сервер работает с **двумя потоками**, каждая операция открывает отдельное соединение SQLite. В `lifecycle_http` две независимые авторизованные сессии одного владельца стартуют через `threading.Barrier`; именно владелец может выполнять оба действия.

- **5 пар освобождений:** в каждой ровно один 200 и один 409. Объявление available; после бронирования другим пользователем запоздалый POST прежнего арендатора получает 409. Снимки **всех пяти таблиц** до/после такого повтора совпадают, включая дату завершения и новое бронирование.
- **5 пар перепродаж с разными ценами:** в каждой один 201 и один 409, в SQLite ровно одно новое объявление для исходного бронирования. Цена совпадает с запросом победителя, продавец правильный, незакрытое объявление объекта ровно одно.
- Искусственный триггер отклоняет UPDATE объявления **после** изменения бронирования: и для освобождения, и для перепродажи получен 500, все таблицы совпадают с состоянием до запроса.
- Отдельно смоделировано несоответствие прежнего статуса объявления: UPDATE меняет ноль строк, ответ 409, завершение бронирования также откатывается.
- Для перепродажи второй триггер отклоняет последний INSERT **после** завершения бронирования и закрытия старого объявления. Ответ 500; прежняя запись снова active без ended_at, прежнее объявление reserved, нового нет. Полные снимки таблиц совпадают. После удаления тестового триггера действие успешно.
- Другое соединение удерживает BEGIN IMMEDIATE: освобождение/перепродажа возвращают **503** за ограниченное время (проверено больше 0,5 и меньше 4 с), без изменений. После снятия блокировки повтор проходит. Это отдельно от гонок 200/201+409; занятость базы не выдается за уже выполненную операцию.

### Рабочая база и сервер — выполнено

- До миграции рабочая база имела схему 3, **1000 properties / 1000 listings / 3000 property_photos / 1 users / 1 bookings**. Уже созданные пользователем данные сохранены.
- Прежний сервер PID 10192 на 8080 и вспомогательный сервер PID 15848 на 8082 остановлены с разрешением. Порт 8080 проверен перед запуском; второй сервер на занятом порту не запускался.
- Через SQLite Backup API создана копия `data/backups/before-stage4-20260912-181732.sqlite3`. Источник открыт только для чтения; копия проверена integrity/fk. Она, как и рабочая база, исключена из Git.
- Один `./build/apartment_server --init-db` применил **только миграцию 4**. Сравнение с резервной копией подтвердило совпадение каждого старого поля и каждой строки во всех пяти таблицах; оба новых поля NULL во всех прежних объявлениях. Схема стала 4, integrity/fk в порядке.
- Обычный запуск снова сохранил все прежние значения: миграции пропущены по версии, генератор не затронул данные. На 8080 работает обновлённый сервер PID **44473**, адрес **http://127.0.0.1:8080/**. Порт 8082 освобождён.
- HTTP `/api/health`: status ok, schema_version 4, foreign_keys true, SQLite 3.53.4, 1000 объектов/объявлений. Каталог: 24 записи на странице, **999 доступных** (одно пользовательское бронирование сохранено). API объявления № 1 вернул три фото и новые поля происхождения; HTML содержит элементы четвёртого этапа.
- Пять полных SELECT из `docs/history-queries.sql` выполнены на рабочей базе в режиме **только чтения**, ошибок SQL нет. Для выбранного объекта без истории соответствующие результаты пустые.
- `git diff --check` и `git check-ignore -v` выполнены: база, резервная копия, WAL/SHM/journal, build, .env/.env.local и ключи игнорируются. Исходники, SQL-миграция, генератор, тесты и документы входят в локальный коммит. Push не выполняется.

### Браузерная проверка этапа 4 — не выполнена

Инструмент обнаружил Safari, но при попытке получить сессию приложения сообщил, что **сессия управления остановлена пользователем**. Дальнейшие действия через браузер не выполнялись. Временный сервер на 8082 остановлен; в его базе не создано ни пользователей, ни бронирований. Визуальные сценарии Safari ниже относятся только к **прошлому этапу 3**. Серверные тесты, проверка HTML и синтаксиса JS не подтверждают DOM-поведение, вёрстку, адаптивность или реальные нажатия новых кнопок.

### Ручная проверка этапа 4 двумя пользователями

1. Откройте **http://127.0.0.1:8080/** в обычном окне (A) и тот же адрес в приватном (B). Войдите под разными email. После обновления сервера вход требуется заново, записи остаются.
2. A бронирует доступную аренду. В «Моих бронированиях» нажмите «Освободить»: сначала попробуйте «Отмена», затем подтвердите «Да, освободить». Запись должна перейти в историю с датой завершения и прежней месячной ценой; ссылка открывает то же доступное объявление.
3. B открывает его URL и нажимает «Арендовать». У B запись активна; у A после обновления остаётся только его завершённая история без кнопки освобождения. Объявление снова «Забронировано». Строго запоздалый повтор запроса A проверен автоматическим HTTP-тестом.
4. A покупает доступное предложение продажи. В кабинете откройте «Выставить на продажу», попробуйте 0, отрицательное/дробное значение, затем введите **55000000** и опубликуйте. Во время запроса повторное нажатие должно быть отключено. Появится ссылка на новый id, старая запись уйдёт в историю с исходной ценой и датой завершения.
5. Откройте старый и новый URL: старый «Закрыто», новый доступен с новой ценой, тем же адресом и фото. У A вместо кнопки покупки пояснение о своём объявлении; у B кнопка «Купить». B бронирует его и видит новую зафиксированную цену в своём кабинете. A не видит чужую запись и не может повторно перепродать через свою историю.
6. Проверьте каталог с фильтрами/сортировкой и переходами страниц; закрытого старого объявления в нём нет. Обновите страницы кабинета и истории, уменьшите окно до ширины телефона: формы, подтверждение, даты, фотографии и ссылки должны оставаться читаемыми.

Ручные действия на 8080 сохраняются в рабочей базе. Для демонстрации без её изменения можно выбрать отдельный `--db` и свободный порт по README. Не удаляйте рабочую базу. Ограничения: учебные сделки без оплаты; сессии одного процесса в памяти на 12 часов, после перезапуска повторный вход; кабинет без пагинации. Избранное, рекомендации и ML не добавлены.

---

## Этап 3 — 12 сентября 2026

Исходная точка — `8e9bc41` (завершённый этап 2). Перед работой проверены инструкции, Git, код, документация и процессы. Рабочая база имела версию 2. Проект и история Git не пересоздавались. Старый сервер PID 95379 продолжал работать на 8080 во время разработки; проверки изменений выполнялись отдельными процессами на свободных портах с временными базами.

### Сборка и автоматические тесты — выполнено

- До изменений существовавшие два теста каталога выполнены: **2/2 прошли**.
- Через Homebrew установлена **libsodium 1.0.22**. Сначала конфигурация с PkgConfig не прошла из-за отсутствия этого инструмента. Сборка исправлена: используется обычный поиск заголовков/библиотеки CMake, PkgConfig проекту не требуется.
- Основной проект собран `cmake --build build -j 4`. Дополнительно выполнены конфигурация с чистым кэшем и полная сборка в `build-stage3-check` с Debug, C++17, Homebrew SQLite/Drogon/libsodium; ошибок и предупреждений компиляции не было.
- После последней правки C++ и теста защиты `/bookings.html` выполнено `ctest --test-dir build --output-on-failure`: **5/5 прошли**, итоговое время 5,86 с. Это `database`, `security`, `booking_database`, `http_smoke`, `auth_http`. Тесты используют `:memory:` или временные отдельные файлы, не рабочую базу.
- Сохранены и выполнены все проверки каталога: совместные фильтры, четыре сортировки, стабильные страницы, фотографии, 400/404, сохранение данных и отсутствие дублей.
- `security`: Argon2id, разные соли для одинакового пароля, правильный/неправильный пароль; замена идентификатора и CSRF сессии, отзыв старой сессии, выход, истечение сессии (в тестовом хранилище срок 0), лимит попыток.
- `booking_database`: фактическая миграция 2 → 3 с сохранением пользовательской цены/описания/фото; нормализованный уникальный email, внешние ключи, соответствие пары объявление/объект, запрет удаления связанного пользователя, запрет двух активных бронирований объявления и объекта, согласованность статуса и даты завершения. Модель завершённого бронирования допускает новое бронирование другим пользователем; завершение выполняется только SQL теста, соответствующего API нет.
- HTTP авторизации: гостевая сессия; регистрация, нормализация email, обычный дубликат и **одновременная регистрация одного email** (один 201, один 409, одна строка); некорректные поля/типы/email/пароль; правильный и неправильный вход; ротация, выход и невозможность повторно использовать старую cookie.
- HTTP доступа: гостю 401 на бронирование и личные данные, перенаправление `/my-bookings` и `/bookings.html` на вход; `user_id` и цена от браузера отвергаются, каждый пользователь видит только свои записи. Зарезервированное объявление доступно по прямому URL, исчезает из доступного каталога.
- CSRF: отклонены POST без токена на регистрации/входе/выходе/бронировании; неверный токен, посторонний Origin, `Sec-Fetch-Site: cross-site`; проверены HttpOnly, SameSite=Lax, Path, Max-Age, удаление cookie. При конфигурации HTTPS проверены флаг Secure и список Origin. Проверены 429 и разделение email-лимитов. Пароль, хеш и выпущенные токены не обнаружены в журнале тестового процесса.

### Гонка и транзакция — выполнено через HTTP

`tests/auth_http.py` запускает настоящий сервер с **двумя рабочими потоками Drogon**. Обработчик каждой операции открывает собственное соединение SQLite. Два разных зарегистрированных пользователя имеют отдельные cookie и CSRF-токены. `threading.Barrier` синхронизирует начало их HTTP-запросов.

Сценарий повторён на пяти свободных объявлениях (21–25). В каждом случае проверено:

- ровно один ответ **201** и один **409**;
- сообщение проигравшему: **«Это жильё уже забронировали»**;
- в SQLite ровно одна запись бронирования, она активна;
- `user_id` записи совпадает с пользователем, получившим 201;
- объявление имеет состояние `reserved`.

Отдельно выполнены успешный и повторный запрос, 404 неизвестного объявления, проверка зафиксированной цены после изменения цены объявления в тестовой базе.

Для проверки отката создан временный `BEFORE INSERT ON bookings` триггер, который выдаёт ошибку **после UPDATE статуса**. Получен 500; объявление осталось `available`, бронирований не появилось. После удаления тестового триггера новое действие прошло.

Для проверки занятости независимое соединение держало `BEGIN IMMEDIATE`: HTTP получил **503** после ограниченного ожидания, статус и записи не изменились. После освобождения блокировки повтор прошёл. Краткая блокировка 350 мс также проверена: запрос смог завершиться после её снятия.

Перезапуск HTTP-сервера и отдельный повторный `--init-db` сохранили **все строки пяти таблиц**. Старые сессии после перезапуска недействительны; после нового входа прежние бронирования снова видны. `integrity_check = ok`, `foreign_key_check` пуст.

### Настоящий браузер — выполнено частично

Первый вызов Safari ожидал разрешения macOS; затем доступ появился. Использован отдельный сервер на свободном порту 8082 и временная база `/private/tmp/apartment-stage3-ui.u9IIYh/test.sqlite3`. В рабочую базу тестовые аккаунты/бронирования не добавлялись. Два тестовых аккаунта созданы через формы, тестовый пароль не сохранён в менеджере паролей.

В Safari визуально и по дереву доступности подтверждены:

- страница объявления с фотографией, адресом, характеристиками, статусом и ссылкой входа для гостя;
- переход на вход и регистрацию с сохранением исходного объявления;
- регистрация первого пользователя и возврат на доступное объявление **без автоматического бронирования**;
- нажатие «Арендовать», сообщение об успехе, состояние «Забронировано», исчезновение кнопки и переход в личный кабинет;
- в «Мои бронирования» три фото, объект, тип аренды, зафиксированная месячная цена, дата и активное состояние;
- регистрация второго пользователя в **приватном окне**, независимость сессий и пустой личный кабинет (чужое бронирование не отображается);
- выход второго пользователя, перенаправление личного кабинета на вход, повторный вход и возврат в кабинет;
- прямое открытие занятого объявления вторым пользователем: оно видно, повторной кнопки нет.

Во время проверки исправлен возврат при переключении между входом и регистрацией через ссылки в шапке: теперь обе сохраняют исходный `next`. Это подтверждено ссылками и фактическим возвращением после регистрации. Синтаксис всех шести JS-файлов также проверен через `vm.Script`; эта проверка отдельно от работы DOM в Safari.

**Не выполнено визуально:** мобильные размеры и другие браузеры, искусственная медленная сеть/ошибки, конфликт двух одновременных нажатий в UI, вариант кнопки «Купить», полный повтор всех сценариев каталога этапа 2. Серверная гонка, продажа/аренда, ошибки и старые тесты выполнены автоматически; это не заменяет перечисленных визуальных проверок. Реальный TLS/reverse proxy и большая нагрузка не проверялись.

### Рабочая база и запущенный сайт — выполнено

- Перед миграцией через SQLite backup API сохранена копия: `data/backups/before-stage3-20260912-131058.sqlite3` (игнорируется Git). Её целостность и все строки сравнены с оригиналом.
- Старый рабочий процесс и временный UI-сервер штатно остановлены. Отсутствие слушателя на 8080/8082 проверено перед запуском обновления; второй сервер на занятом порту не запускался.
- `./build/apartment_server --init-db` выполнен **дважды**. Рабочая схема стала 3. Все строки `properties`, `listings`, `property_photos` сравнены с резервной копией: полное совпадение, **1000 / 1000 / 3000**. `users` и `bookings` пусты; рабочие данные не использовались для тестового бронирования.
- `PRAGMA integrity_check` вернул `ok`; `PRAGMA foreign_key_check` — пусто. Из руководства выполнены 18 одиночных SELECT/EXPLAIN-примеров через соединение только для чтения (параметры `?` заменялись тестовыми значениями); ошибок SQL нет.
- Обновлённый сервер запущен на **http://127.0.0.1:8080/**. `/api/health` подтвердил схему 3, SQLite 3.53.4, внешние ключи и 1000/1000 строк. Отдельные HTTP-запросы подтвердили 24 карточки из 1000 и объявление № 1 с тремя фото. Первая попытка этой дополнительной проверки через Python была запрещена песочницей; повтор с разрешением прошёл.
- Проверены `git diff --check` и исключения: рабочая база, WAL/SHM/journal, сборки, `.env`/`.env.local`, ключи игнорируются. Исходники, SQL, генератор, тесты и документация включаются в коммит этапа. Push не выполняется.

### Ручная проверка двумя пользователями

1. Откройте `http://127.0.0.1:8080/` в обычном окне. Выберите доступное объявление и сохраните его URL. Как гость перейдите по ссылке входа, зарегистрируйте пользователя A; убедитесь, что вернулись к этому объявлению и оно ещё доступно.
2. Откройте **тот же адрес и порт** в приватном окне. Зарегистрируйте пользователя B с другим email. Его «Мои бронирования» должны быть пустыми. Вернитесь по сохранённому URL.
3. Пока в обоих окнах видна кнопка, нажмите «Арендовать»/«Купить» сначала в одном и сразу в другом. Одно окно должно показать успех, другое — «Это жильё уже забронировали» и обновлённый статус. Последовательные ручные нажатия проверяют UI конфликта; строго одновременную гонку доказывает автоматический HTTP-тест с барьером.
4. Откройте «Мои бронирования» в обоих окнах: объект есть только у победителя, указаны фотографии, цена на момент бронирования, дата и состояние. Сумма — в драмах, аренда подписана «в месяц».
5. Обновите прямую страницу занятого объявления в обоих окнах: она открывается, статус «Забронировано», кнопки действия нет. В доступном каталоге этого объявления больше нет.
6. Выйдите из одного аккаунта. `/my-bookings` должен снова требовать входа. Проверьте неверный пароль, затем войдите с правильным: собственные записи сохраняются. Вход в другом окне не должен измениться.
7. При желании остановите сервер через Ctrl+C и запустите заново: `./build/apartment_server`. Оба пользователя должны войти повторно; бронирования остаются. Освобождения и перепродажи на этом этапе нет, для следующей попытки выберите другое свободное объявление.

Ручные действия на 8080 сохраняют ваши новые аккаунты и бронирования в рабочей базе. Для многократной демонстрации без её изменения используйте отдельный `--db` и свободный порт по инструкции README. Не удаляйте рабочую базу ради повторения теста.


## Этап 2 — 12 сентября 2026

Этап развивает коммит `800b439`: проект не пересоздавался. Никакие функции регистрации, бронирования и ML не добавлялись.

### Что выполнено и проверено

- До изменений выполнены существовавшие тесты первого этапа: **2/2 прошли**.
- Собрана обновлённая программа C++17 (`cmake --build build -j 4`). Финальный прогон расширенных тестов: **2/2 прошли**, `database` и `http_smoke`.
- C++ тесты: схема 2, внешние ключи на каждом соединении, 1000 объектов/1000 первоначальных объявлений/3000 фото, 500 аренд и 500 продаж, параметры SQL, ограничения данных, запрет двух незакрытых объявлений одного объекта, сохранение изменённых цен/описаний/истории, полный откат при ошибке заполнения.
- Проверена миграция **версии 1 → 2** на отдельной базе с изменённым демообъектом, произвольными `id`, закрытым объявлением и пользовательской фотографией. Всё сохранено после миграции и повторного генератора. Неизвестная будущая версия не сбрасывается.
- HTTP-тест сравнил все 1000 id с независимой сортировкой данных в Python для **четырёх вариантов** порядка. В наборе есть равные цены/площади. Обход страниц по 37 объявлений прошёл без повторов, пропусков и нарушений порядка; `id ASC` стабилизирует равенство.
- Совместные фильтры сделки, района, минимальной/максимальной цены и комнат проверены с каждым из четырёх порядков. Проверены включённые границы цены, полный `COUNT`, метаданные пагинации, пустые результаты, далёкая страница, размер 24 по умолчанию и максимум 100.
- Проверены понятные JSON-ответы **400** для неверных чисел, границ, сортировок, неизвестных/пустых/повторных параметров, неправильного URL-кодирования; также проверены попытки SQL-инъекции.
- Проверены `/api/listings/{id}`, прямой `/listings/{id}` и повторный запрос этой HTML-страницы, полный набор полей и три фото, API/HTML **404**, неверный id **400**, доступность закрытого объявления по прямому адресу и отсутствие его в каталоге, объявление без фото.
- Проверены HTML, CSS, все три JavaScript-файла, SVG-заглушка и все четыре локальных JPEG: HTTP 200, JPEG-сигнатуры. Каталог использует ограниченный набор повторяющихся иллюстраций, а не реальные фотографии адресов.
- Проверены изменение SQLite → изменение JSON, перезапуск процесса и сравнение всех строк, повторный `--init-db`, сохранение дополнительного пользовательского объекта и нового объявления сверх 1000. Два независимых новых демонабора совпали по всем данным кроме реального времени создания.
- Все автоматические тесты выполнялись в памяти или на отдельных временных файлах. Рабочая база не использовалась для тестовых изменений.
- Перед обновлением рабочей базы сделана локальная резервная копия в `data/backups/`. Снимок старых **12 объектов / 12 объявлений / 36 фото** сравнен по каждому полю после расширения: старые строки полностью сохранены. Теперь в рабочей базе **1000 / 1000 / 3000** строк; повторный генератор не изменил ни одной строки. `integrity_check` → `ok`, `foreign_key_check` → пусто.
- Выполнены восемь SQL-примеров и их `EXPLAIN QUERY PLAN` на SQLite 3.53.4 в режиме только чтения. Планы сохранены в [query-plans.md](query-plans.md), запросы — в [catalog-queries.sql](catalog-queries.sql). Для сортировки по площади план использует временную сортировку; это отражено в руководстве.
- Проверен синтаксис `common.js + app.js` и `common.js + listing.js` через `vm.Script`. Это проверка синтаксиса, **не выполнение DOM и не браузерный тест**.
- Старый сервер остановлен; перед каждым последующим запуском проверялось освобождение порта. На `http://127.0.0.1:8080/` запущен один сервер проекта. Запрос health подтвердил схему 2, 1000/1000 строк и включённые внешние ключи; совместный фильтр аренды в Кентроне, 200–500 тысяч, 2 комнаты, `area_desc` вернул 11 правильно отсортированных объявлений.

### Что не проверено в браузере

Инструмент управления Safari вызван, но вернул **Computer Use permissions are not granted**. Поэтому визуальный вид, адаптивность, нажатия кнопок, восстановление URL браузером, переключение фотографий и отображение ошибок/заглушек в настоящем браузере пока **не проверены**. Успешные HTTP-тесты подтверждают серверную часть этих сценариев и отдачу файлов, но не их визуальное отображение.

### Семь ручных проверок

1. Открыть `http://127.0.0.1:8080/`: найдено 1000 объявлений, на первой странице 24 карточки с фотографиями. Сузить окно — карточки и форма должны перестроиться без горизонтальной прокрутки.
2. Выбрать «Аренда», район «Кентрон», цену от 200000 до 500000 и 2 комнаты, нажать «Показать»: ожидается 11 объявлений, у цен подпись «в месяц». Переключить продажу и проверить, что тип сделки меняет результат.
3. Нажать «Сбросить» и последовательно выбрать четыре сортировки: цена и площадь должны идти в выбранном направлении.
4. Перейти на страницу 2, скопировать URL и открыть его в новой вкладке, затем обновить. Условия и номер страницы должны сохраниться. Изменить фильтр и нажать «Показать» — вернуться на страницу 1; проверить «Назад/Вперёд» браузера.
5. Нажать фото или заголовок карточки, переключить миниатюры, проверить описание, адрес, характеристики и статус. Обновить страницу объявления; «Вернуться в каталог» должно сохранить исходную выборку и страницу.
6. Открыть `/listings/99999999`: должно появиться «Объявление не найдено» и ссылка в каталог. Открыть `/?page=0`: понятная ошибка параметра и работающая кнопка сброса.
7. Задать максимальную цену 1 и нажать «Показать»: пустая выдача с предложением изменить условия. Нажать «Сбросить» — снова 1000 результатов.

Рабочую базу при ручных проверках менять не требуется. Сценарий недоступного фото предусмотрен кодом заглушки; его визуальная проверка потребует отдельного тестового окружения или блокировки изображения в инструментах браузера.

## Этап 1 — архив проверки от 11 сентября 2026

Дата: 11 сентября 2026. Среда: macOS 15.0, arm64.

### Подтверждено выполнением

- Проверены Apple Clang 15.0.0, CMake 4.3.4 и Git 2.39.3. Через Homebrew установлены Drogon 1.9.13 и SQLite 3.53.4 с зависимостями.
- Проект сконфигурирован и собран. В `compile_commands.json` подтверждён `-std=c++17` для кода проекта. `otool -L` подтвердил подключение библиотек Drogon и Homebrew SQLite.
- Дополнительно выполнена чистая конфигурация и сборка в новой папке `build-clean` с командой поиска зависимостей из README: успешно, без старого кэша CMake.
- `ctest --test-dir build --output-on-failure`: **2/2 теста прошли** (`database`, `http_smoke`). HTTP-тест выполнен с разрешением на локальный порт после отказа песочницы.
- C++ тест проверил ограничения схемы, внешние ключи на двух соединениях, параметры SQL, сохранение изменённых данных и закрытой истории при повторном заполнении, полный откат заполнения при искусственной ошибке.
- HTTP-тест запустил настоящий сервер с временной SQLite-базой: API, HTML, CSS, JavaScript и JPEG отдаются; аренда/продажа дают по 6 объявлений; неверный тип даёт 400, POST к listings даёт 405, файл базы не доступен по `/data/apartments.sqlite3`.
- Изменения цены и описания непосредственно в временной SQLite-базе появились в JSON. Закрытое объявление исчезло из доступных. Добавленный пользовательский объект сохранился после перезапуска.
- HTTP-тест остановил процесс, запустил его повторно и сравнил все строки трёх таблиц. Дубли и потери не обнаружены. Дополнительный `--init-db` также сохранил строки без изменений.
- `PRAGMA integrity_check` временной базы вернул `ok`, `PRAGMA foreign_key_check` — пустой результат.
- Рабочая база подготовлена двумя последовательными командами `./build/apartment_server --init-db`: оба раза 12 объектов и 12 объявлений. Прямой запрос SQLite подтвердил **12 / 12 / 36** строк в `properties` / `listings` / `property_photos`.
- Рабочий сервер запущен на `127.0.0.1:8080`. Отдельный `curl /api/health` подтвердил `status: ok`, `foreign_keys: true`, схему 1, SQLite 3.53.4 и 12/12 записей. Отдельный запрос аренды вернул 6 объектов с `deal_type = rent`.
- Синтаксис `public/app.js` проверен компиляцией через JavaScript `vm.Script`; ошибок синтаксиса нет. Это не проверка выполнения DOM в браузере.
- Создан отдельный локальный Git-репозиторий в папке проекта. `git check-ignore -v` подтвердил исключение рабочей базы, сборки, `.env`, `.env.local` и файла ключа. В списке исходников для Git нет рабочей базы и сборки. Коммиты и публикация на GitHub не выполнялись.

### Ограничения проверки

Визуальный просмотр в Safari и нажатия кнопок в настоящем браузере **не выполнены**: инструмент управления компьютером сообщил, что разрешения macOS Accessibility / Screen Recording не предоставлены. Автоматизированные проверки подтверждают сервер, данные, ресурсы и синтаксис JavaScript, но не заменяют проверку вёрстки, мобильного вида и DOM-поведения.

Ручная проверка: открыть `http://127.0.0.1:8080/`, убедиться в появлении 12 карточек и фотографий; выбрать «Аренда» и «Продажа» — в каждом разделе должно быть 6 карточек; вернуться на «Всё жильё». При уменьшении ширины окна каталог должен переходить с трёх колонок на две и одну.

Проверки регистрации, одновременного бронирования и перепродажи не выполнялись: эти функции намеренно не реализованы на первом этапе.
